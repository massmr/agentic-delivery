{
  "name": "ewokbot",
  "version": "0.1.0",
  "description": "MCP-first autonomous software delivery runtime with human-gated production approval.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "bin": {
    "ewokbot": "./dist/src/cli/index.js",
    "ewok": "./dist/src/cli/index.js",
    "agentic": "./dist/src/cli/index.js"
  },
  "files": [
    "dist",
    "config",
    ".env.example",
    "assets"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "pnpm run build && node --test dist/test/*.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
